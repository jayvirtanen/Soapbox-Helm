pipeline {
  agent any
  parameters {
  string defaultValue: 'latest', name: 'version'
	}
  stages {
    stage('Docker Build') {
      steps {
      }
    }
    stage('Push Docker Image'){
        steps{
        }
    }
    stage('Deploy with Helm') {
      steps {
        }
      }
    }
  }